# Base URL of the API.
#   local dev  -> http://localhost:3000
#   Vercel Production env -> the prod Render URL (olc-api)
#   Vercel Preview env    -> the dev Render URL (olc-api-dev)
# See docs/adr/0007-dev-prod-environments.md
VITE_API_URL=http://localhost:3000
